#include <stdio.h>
#include <stdlib.h>
#define size 20

struct T
{
	int data;
	struct T * left;
	struct T * right;
};

typedef struct T tree;

int main(int argc, char const *argv[])
{
	tree * head;
	head->data = NULL;
	return 0;
}
